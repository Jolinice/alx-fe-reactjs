// alx-react-app-props/src/components/UserProfile.jsx
// This file is unused by the Context refactor, so we reset it to a simple state.
const UserProfile = (props) => {
  return (
    <div style={{ display: 'none' }}>
      {/* This component is ignored for the Context task */}
    </div>
  );
};

export default UserProfile;